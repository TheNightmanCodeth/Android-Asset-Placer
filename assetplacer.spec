# -*- mode: python -*-
a = Analysis(['/home/joe/Documents/Assetplacer/assetplacer.py'],
             pathex=['/home/joe/Documents/Assetplacer'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='assetplacer',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='assetplacer')
